import {CardModel} from "./models";
import {ModelController} from "../libs/controller";

export class CardModelController extends ModelController {
    constructor() {
        super(CardModel);
    }
}